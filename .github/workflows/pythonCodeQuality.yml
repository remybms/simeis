name: Python Code Quality

on:
    pull_request

jobs:
    build:
        runs-on: ubuntu-latest
    
        steps:
            - uses: actions/checkout@v4

            - run: |
                pip install ruff
                ruff check tests/propertybased.py