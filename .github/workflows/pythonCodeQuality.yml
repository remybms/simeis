name: Rust Code Quality

on:
    push

jobs:
    build:
        runs-on: ubuntu-latest
    
        steps:
            - uses: actions/checkout@v4

            - run: |
                pip install black
                black .